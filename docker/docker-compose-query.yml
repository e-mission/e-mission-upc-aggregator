version: "3"
services:
  query:
    image: emission/test-query:1.0
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    ports:
      #ENV VAR FOR RUNTIME PORT MAPPING
        - $PORTMAP
    networks:
      - emission
networks:
  emission:
    external: True
